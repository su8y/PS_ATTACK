package algorithm_mg.dfs_bfs;

public class 게임내최단거리 {
    // 갈 수 있는 곳은 true(1), 없는곳은 false(0)인 2차원 배열이 주어짐
    // 방문자는 true만 지나가면서 최단 거리를 구해야함
    // true를 지나갈때만 ++을 하고 마지막 골은 배열의 마지막 칸으로 설정
    // 기본적으로 오른쪽(열을) 이동하면서 진행 false가 나오면 행을 증가시켜서 재귀함수 실행
    // 이를 반복하여 배열의 끝으로 갈떄까지 진행
}
